import { IconDefinition } from "@fortawesome/free-solid-svg-icons";

type SubEventType = {
    id: number;
    title: string;
    icon: IconDefinition;
    color: string;
};
export default SubEventType;
